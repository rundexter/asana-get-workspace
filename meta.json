/*
 * Metadata describing the module operation
 */
{
    //Required. Inputs the module expects.
    "inputs": [{
        "id"          : "filter_name",
        "title"       : "Name Filter",
        "description" : "The workspace name to filter by"
    }],
    //Required. Outputs the module emits.
    "outputs": [{
        "id"    : "id",
        "title" : "Workspace ID"
    }, {
        "id"    : "name",
        "title" : "Workspace Name"
    }],
    "title": "Asana Get Workspaces" /* Required.A display name for the module */
}
